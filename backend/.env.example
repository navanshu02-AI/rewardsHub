# Required in non-development environments
SECRET_KEY=

# Optional: AI features are disabled if unset
GEMINI_API_KEY=

# Database configuration
MONGO_URL=
DB_NAME=

# Environment: opt into development defaults
ENV=development

# CORS configuration (example)
BACKEND_CORS_ORIGINS=http://localhost:3000

# Expose reset token in responses (false unless dev)
EXPOSE_RESET_TOKEN_IN_RESPONSE=false
